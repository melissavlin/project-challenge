<div class="col-sm-12 col-md-6 col-lg-4">
	<a href="<%= url_for(dog) %>">
	  <article>
	    <h2 class="dog-name"><%= dog.name %></h2>
	    <%= image_tag url_for(dog.images.first), class: "dog-photo img-fluid w-100", alt: "Photo of #{dog.name}" %>
	  </article>
	</a>
</div>
